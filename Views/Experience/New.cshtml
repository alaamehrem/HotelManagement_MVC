@* @model Experience
 *@
 @model ExperienceWithTypesViewModel
@{
    ViewData["Title"] = "New";
    Layout = "_Index";
}

<!-- Breadcrumb Section Begin -->
<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <div class="breadcrumb-text">
                <h2>Create New Experience</h2>
                <div class="bt-option">
                    <a asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                    <a asp-area="" asp-controller="Experience" asp-action="Index">All Experiences</a>
                    <span>Create New</span>
                </div>
            </div>
        </div>
    </div>
</div>
<br />
<!-- Breadcrumb Section End -->

<!-- Blog Section Begin -->
<section class="blog-section blog-page spad">

    <div class="container">
        <h3>Experience Form</h3>
        <br />
        <form asp-action="SaveNew" asp-controller="Experience" method="post" enctype="multipart/form-data">
            <div>
                <label asp-for="ExperienceName"> </label>
                <input asp-for="ExperienceName" class="form form-control" />
            </div>
            <div>
                <label asp-for="Image"> </label>
                <input Name="FileImage" type="file" class="form form-control" style="border:none; outline:none;" />
            </div>
            <br />
            <div>
                <label asp-for="CoverImage"> </label>
                <input Name="FileCoverImage" type="file" class="form form-control" style="border:none; outline:none;" />
            </div>
            <br />
            <div>
                <label asp-for="Type"> </label>
                <br />
                <select name="Type" id="Type" class="form form-control">
@*                     @if (Model.Type != null && Model.Type.Experiences != null)
                    {
                        foreach (var typeItem in Model.Type.Experiences)
                        {
                            if (Model.TypeId == typeItem.Id)
                            {
                                <option value="@typeItem.Id" selected>@typeItem.Type.Name</option>
                            }
                            else
                            {
                                <option value="@typeItem.Id">@typeItem.Name</option>
                            }
                        }
                    }
                    else
                    {
                        <option value="">No Types Available</option>
                    } *@
                    
@*                     @foreach (var Item in Model.Experiences)
                    {
                        if (Model.TypeId == Item.TypeId && Model.ExperienceId != Item.Id)
                        {
                            <option value="@Item.TypeId" selected>@Item.Type.Name</option>
                        }
                        else
                        {
                            <option value="@Item.TypeId"> @Item.Type.Name </option>
                        }
                    } *@

@*                     @{
                        var addedTypeIds = new List<int>(); 
                    }
                    @foreach (var Item in Model.Experiences)
                    {
                        if (!addedTypeIds.Contains(Item.TypeId))
                        {
                            <option value="@Item.TypeId"> @Item.Type.Name </option>
                            addedTypeIds.Add(Item.TypeId); 
                        }
                    } *@

                    @foreach (var Item in Model.Type)
                    {
                    <option value="@Item.Id"> @Item.Name </option>

                    }
                   

                </select>
            </div>
            <br />
            <br /> 
            <div>
                <label asp-for="Description"> </label>
                <input asp-for="Description" class="form form-control" />
            </div>
            <br />
            <div>
                <label asp-for="Price"> </label>
                <input asp-for="Price" class="form form-control" />
            </div>
            <br />
            <div>
                <label asp-for="Duration"> </label>
                <input asp-for="Duration" class="form form-control" />
            </div>
            <br />
            <div>
                <label asp-for="instructions"> </label>
                <input asp-for="instructions" class="form form-control" />
            </div>
            <br />
            <div>
                <label asp-for="Requirements"> </label>
                <input asp-for="Requirements" class="form form-control" />
            </div>
            <br />
            <input type="submit" value="Create" class="btn btn-success" />
        </form>
        <br />
        <a asp-action="Index" asp-controller="Experience" class="btn-light" >Back to list</a>
    </div>
</section>

